/* Task
Write an SQL query to retrieve two columns using the 
SELECT statement, specifically: region and capital. Sort the 
result first by region and then by capital.  */

SELECT region, capital
FROM country
ORDER By region, capital;
